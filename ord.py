# Python3 program to Split string into characters
def split(word):
    return [char for char in word]
def toNum():
    for i in range(len(ar)):
        for j in range(len(ar[i])):
            ar[i] = ord(ar[i][j])-96
def convert(lst):
    return (lst[0].split())
# Driver code
st = ['This is Halloween']
ar = convert(st)
for k in range(len(ar)):
    ar[k] = split(ar[k])

print(ar[0][1]+ar[0][2])
